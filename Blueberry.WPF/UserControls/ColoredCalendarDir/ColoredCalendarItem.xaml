<UserControl x:Class="Blueberry.WPF.UserControls.ColoredCalendarItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Blueberry.WPF.UserControls"
             mc:Ignorable="d">
    <Border Padding="5" Margin="3,5" CornerRadius="5" BorderThickness="2" BorderBrush="Gray">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
        
            <TextBlock Grid.Row="0"
                       Text="{Binding Path=Day}" 
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        
            <Rectangle Grid.Row="1"
                       Fill="{Binding Color}"/>
        
            <TextBlock Grid.Row="2"
                       Text="{Binding Path=Amount}" 
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
