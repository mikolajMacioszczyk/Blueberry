<UserControl x:Class="Blueberry.WPF.UserControls.HarvestControls.HarvestItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Blueberry.WPF.UserControls.HarvestControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" 
                   Text="{Binding Path=Employee, StringFormat=Pracownik: {0}}" 
                   FontSize="22"/>
        
        <TextBlock Grid.Column="1"
                   Text="{Binding Path=Amount, StringFormat=Ilość: {0}}"
                   FontSize="22"/>
        
    </Grid>
</UserControl>
